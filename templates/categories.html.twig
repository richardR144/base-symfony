{% extends 'base.html.twig' %}

{% block metatitle %}  Catégories {% endblock metatitle %}

{% block main %}
    <section>
        {% for category in categories %}
            <article>
                <h2>{{ category.title }}</h2>
                <p>{{ category.color }}</p>
            </article>
        {% endfor %}
    </section>
{% endblock main %}